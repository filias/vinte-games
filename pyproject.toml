[project]
name = "vinte-games"
version = "0.1.0"
description = "Chytaj Jablká - a retro apple catching game"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "gunicorn>=22",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
